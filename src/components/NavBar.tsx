import { Button } from "./ui/button";

export const NavBar = () => {
  return (
    <nav className="absolute top-0 flex-1 flex-row items-center justify-between flex-wrap bg-background p-4 w-full"></nav>
  );
};
